<h1>Upload Photo</h1>

<%= form_for @photo do |f| %>
  <!-- (render error messages) -->
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :image %>
  <%= f.file_field :image %>

  <%= f.submit 'Upload' %>

<% end %>

<%= link_to "Back", photos_path %>